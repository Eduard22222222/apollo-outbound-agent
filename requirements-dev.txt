# The repo itself needs NOTHING at runtime - scripts/ and mcp/ are standard library only,
# on Python 3.11+ (tomllib).
#
# These are for development and for the optional XLSX importer.
pytest>=8.0
ruff>=0.6
openpyxl>=3.1   # optional: only needed by import_master.py for .xlsx input
